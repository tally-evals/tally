{
  "name": "tally-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@8.15.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "msw": "^2.12.1",
    "turbo": "^1.12.4"
  },
  "dependencies": {
    "ai": "^5.0.89",
    "zod": "^4.1.12"
  }
}
