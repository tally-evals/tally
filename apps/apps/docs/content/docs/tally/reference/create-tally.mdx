---
title: createTally
description: API reference for the createTally function.
---

# `createTally()`

Create a Tally runner instance for evaluating datasets with one or more evaluators.

## Import

```ts
import { createTally } from '@tally-evals/tally';
```

## API Signature

import { TypeTable } from 'fumadocs-ui/components/type-table';

### Parameters

<TypeTable
  type={{
  "args": {
    "description": "Arguments for creating the Tally instance.",
    "type": "{ data: readonly TContainer[]; evaluators: readonly Evaluator<MetricContainer>[] }",
    "required": true,
    "parameters": [
      {
        "name": "data",
        "description": "Array of containers (DatasetItem[] or Conversation[]).",
        "type": "readonly TContainer[]",
        "required": true
      },
      {
        "name": "evaluators",
        "description": "Array of evaluators (with evals).",
        "type": "readonly Evaluator<MetricContainer>[]",
        "required": true
      }
    ]
  }
}}
/>

### Returns

<TypeTable
  type={{
  "tally": {
    "description": "A Tally runner instance.",
    "type": "Tally<TContainer>",
    "required": true
  }
}}
/>

## Learn more

- [Getting Started](/docs/tally/getting-started)
